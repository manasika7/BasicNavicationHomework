package com.cbt.test.practice;

import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;

public class practice1 {
    public static void main(String[] args) {
/*        driver.findElement(By.name("username")).sendKeys("Test User");

        //Step 4. Enter any valid email
        driver.findElement(By.name("password")).sendKeys("test_email@email.com");
        //Step 5. Click on “Sign Up” button
        driver.findElement(By.name("login")).click();
        //wait
        Thread.sleep(3000);
        /*
                Expected result: Following message should be
                displayed: “Thank you for signing up. Click the
                button below to return to the home page.” Home
                button should be displayed

                 String message= driver.findElement(By.xpath("//*[@id=\"content\"]/div/h3"));
             //wait
             Thread.sleep(3000);
               System.out.println(message.getText());
               */
        }




}
